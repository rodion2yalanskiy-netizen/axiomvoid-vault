---
tags: [отчёт, выполнено, ai-pipeline]
date: 2026-05-22
status: completed
---

## ✅ Выполнено

- Добавлена плавающая CTA-кнопка «Консультация» в правом нижнем углу сайта
- Кнопка появляется через 3 секунды после загрузки страницы (через `setTimeout`)
- Клик по кнопке ведёт к секции `#contact` (плавный скролл браузера)
- На мобиле (≤ 768px) текст скрыт — отображается только иконка телефона, кнопка становится круглой
- Цвет: золотой `#d4a843`, текст: тёмный `#1a1a1a`
- Шрифт адаптирован под проектный дизайн-систему: `var(--font-sans)` (Outfit) вместо Inter
- Добавлена поддержка `prefers-reduced-motion` — анимация hover отключается при сниженной активности
- Кнопка изначально скрыта (`opacity: 0; pointer-events: none`) — не мешает взаимодействию

## 📁 Изменённые файлы

- `index.html` — добавлено 58 строк:
  - CSS-блок `/* === Floating CTA Button === */` внутри `<style>`
  - HTML-элемент `<a class="cta-float" id="ctaFloat">` перед `</body>`
  - JS `setTimeout` в существующем `<script>`-блоке

## 🔗 Git коммит

`f55adae` — feat: добавить плавающую CTA-кнопку «Консультация» (появляется через 3 сек)
`bf69626` — feat: добавить плавающую CTA-кнопку «Консультация» (финальный: +:focus-visible, a11y fix)

Ветка: `main` → https://github.com/rodion2yalanskiy-netizen/premium-tiling-website
