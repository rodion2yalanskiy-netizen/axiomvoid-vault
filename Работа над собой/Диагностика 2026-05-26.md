---
date: 2026-05-26
type: diagnostic
auto: true
tags: [диагностика, система, агенты]
---

# 🔧 Диагностика архитектуры — 2026-05-26

**Дата:** 2026-05-26 09:22 | **Выполнил:** Claude Code (ручной запуск)

---

## ✅ Проверено — всё работает

| Компонент | Результат |
|-----------|-----------|
| `local-agent.sh` — bash синтаксис | ✅ OK |
| `vault-sync.sh` — bash синтаксис | ✅ OK |
| `daily-self-dev.sh` — bash синтаксис | ✅ OK |
| `bot.py` — Python синтаксис | ✅ OK |
| `analyzer.py` — Python синтаксис | ✅ OK |
| Telegram API | ✅ `@Improvement_for_mi_bot` отвечает |
| git repo Бизнес QSNera | ✅ чист, up-to-date |
| local-agent heartbeat | ✅ каждые 5 мин (последний: 09:18) |
| daily-self-dev сегодня | ✅ запущен 09:00, задачи 1+2 ✅, задача 3 в процессе |
| launchd плисты (4 шт) | ✅ все загружены |
| Claude binary | ✅ `/Users/rodionyalanskiy/.nvm/versions/node/v24.16.0/bin/claude` |
| `obsidian-sync-fix.py` | ✅ существует |
| Vault структуры (3 vault'а) | ✅ все папки на месте |

---

## ⚠️ Найденные проблемы

### 1. Задача застряла в папке `📝 Задачи/` (emoji-папка)

**Где:** `~/vaults/Бизнес QSNera/📝 Задачи/Задачи QSNera (0713).md`
**Что нашёл:** Файл задачи лежал в папке с emoji-префиксом, которую `local-agent.sh` не сканирует (он смотрит только `Задачи/` без emoji). Задача висела необработанной с 26.05.
**Причина:** Повторяющаяся проблема — папка `📝 Задачи/` периодически воссоздаётся. Скорее всего git-история содержит её и при merge/fetch она восстанавливается.
**Исправил:** Файл перемещён в `Задачи/`, изменения запушены в GitHub (`9c66246`). Задача будет обработана local-agent при следующем запуске (в течение 5 мин).

---

### 2. Brain vault — 80+ микро-коммитов в день

**Где:** `~/vaults/Цифровой мозг/Brain/session-state.md` + `Brain/memory/episodic/2026-05-26.md`
**Что нашёл:** vault-sync.sh пушит эти 2 файла каждые 5 минут — т.к. session-state.md меняет только timestamp (`updated: ДАТА ЧАС:МИН`). За сегодня: уже **80 коммитов** (и это только до 09:22).
**Причина:** session-state.md обновляется хуком при каждом старте/стопе сессии, плюс `obsidian-sync-daemon` или другой агент дополняет episodic-память. vault-sync.sh коммитит все изменения без фильтрации.
**Исправил:** Не исправлял — функционально работает. Рекомендация ниже.

---

### 3. daily-self-dev — задачи завершаются ровно на 600с

**Где:** `~/.claude/agents/daily-self-dev.sh`, переменная `TASK_TIMEOUT=600`
**Что нашёл:** Все 3 задачи в логе показывают точно 600с или 601с выполнения. Это пограничное значение — Claude завершается ровно в момент timeout.
**Причина:** Для сложных задач (самоанализ всей системы, веб-поиск) Claude реально нужно ~10 мин. Задачи выполняются, но результат может быть неполным.
**Исправил:** Не исправлял немедленно — задачи всё же возвращают вывод (exit code 0). Рекомендация ниже.

---

### 4. Исторические ошибки vault-sync (май 23-25) — РЕШЕНО

**Что было:**
- `GH013: Repository rule violations` — пытался пушить секрет в `.obsidian/sync.json`
- `Cannot rebase onto multiple branches` — конфликт при merge
- `unstaged changes` при rebase

**Статус:** Все ошибки относятся к периоду 23-25 мая. Текущие логи чистые — только успешные push'и.

---

## 🔧 Выполненные исправления

1. **Перемещена задача** `📝 Задачи/Задачи QSNera (0713).md` → `Задачи/Задачи QSNera.md`
   - Коммит: `9c66246` — `fix: перенос задачи из 📝 Задачи → Задачи (повторная проблема emoji-папки)`
   - Задача будет автоматически обработана local-agent через ≤5 мин

---

## 📊 Состояние системы на 2026-05-26

| Компонент | Статус | Последняя активность | Проблемы |
|-----------|--------|---------------------|---------|
| `local-agent` | ✅ Работает | 09:18 (heartbeat) | Нет задач до сегодня |
| `vault-sync` | ✅ Работает | 09:17 (Brain push) | 80+ коммитов/день |
| `daily-self-dev` | ✅ Запущен | 09:20 (задача 2/3) | Timeout 600с впритык |
| `obsidian-sync` | ✅ Загружен | — | — |
| Telegram бот | ✅ Online | — | — |
| git Бизнес QSNera | ✅ Чист | 09:22 (наш коммит) | — |
| git Цифровой мозг | ✅ Активен | 09:17 | 80+ коммитов/день |
| Python бот | ✅ Синтаксис OK | — | — |
| Vault структуры | ✅ Все папки | — | `📝 Задачи/` — повторная проблема |

---

## 💡 Рекомендации на ближайшие дни

### 🔴 Высокий приоритет

**1. Защита от emoji-папки `📝 Задачи/`**
Это третий раз когда папка появляется (git log: `0c3e0ef`, `0d6978a`, `9c66246`). Нужно добавить в `vault-sync.sh` авто-детектор и авто-перенос:
```bash
# После git pull — проверяем и исправляем emoji-папку
if [ -d "$VAULT/📝 Задачи" ]; then
    for f in "$VAULT/📝 Задачи"/*.md; do
        [ -f "$f" ] && mv "$f" "$VAULT/Задачи/"
    done
    rmdir "$VAULT/📝 Задачи" 2>/dev/null
    echo "$(timestamp) 🔧 Auto-fix: перенос задач из emoji-папки" >> "$LOG"
fi
```

### 🟡 Средний приоритет

**2. Снизить частоту Brain-коммитов**
80 коммитов/день только для обновления timestamp в session-state.md — это избыточно. Варианты:
- Добавить проверку: коммитить только если файл изменился более чем на строку timestamp
- Использовать `--allow-empty=false` по-умолчанию
- Не трекать `session-state.md` в git (добавить в `.gitignore`)

**3. Увеличить `TASK_TIMEOUT` в daily-self-dev.sh**
С 600с до 900с (15 мин) — задачи самоанализа требуют веб-поиска и записи в несколько файлов:
```bash
TASK_TIMEOUT=900  # было 600
```

**4. Зафиксировать версию Node в plist-файлах через symlink**
`/Users/rodionyalanskiy/.nvm/versions/node/v24.16.0/bin` — hardcoded версия. При обновлении node plist сломается.
Решение: добавить симлинк `~/.nvm/alias/default` → latest и использовать его в PATH.
