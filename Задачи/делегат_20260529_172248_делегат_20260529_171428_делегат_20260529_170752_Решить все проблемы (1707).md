---
status: delegated
tool: code
task_name: делегат_20260529_171428_делегат_20260529_170752_Решить все проблемы (1707)
source_file: Задачи/делегат_20260529_171428_делегат_20260529_170752_Решить все проблемы (1707).md
delegated_at: 2026-05-29T17:22:48.733084+00:00
---

# Решить все проблемы

## 🎯 Задача
Провести полный аудит и исправление системы: Railway GitHub webhook, команды бота, daily-self-dev.sh (шаг 3), и ночные падения vault-sync/local-agent. Устранить все критические и важные находки из отчёта диагностики от 2026-05-29.

## 🤖 AI Dispatcher
Перед выполнением прочитай ~/.claude/agents/dispatcher.md и активируй нужных агентов:
Агенты для этой задачи: **DevOps | Backend Dev | QA Tester | Tech Lead**
Tech Lead делает финальное ревью после всех агентов.

## 📋 Шаги выполнения

### БЛОК 1: Railway GitHub Webhook (DevOps)
1. Открыть документацию Railway и проверить текущий статус webhook: `railway status` в директории `~/Desktop/qsnera-reels-bot/`
2. Зайти в Railway Dashboard → проект бота → Settings → Source → подключить GitHub репозиторий qsnera-reels-bot
3. Убедиться что Auto Deploy включён для ветки `main`
4. Проверить что последний коммит задеплоен: сравнить хэш `git log --oneline -1` с Railway Deployments
5. Выполнить тестовый `git push` и убедиться что Railway автоматически запускает деплой

### БЛОК 2: Кириллические команды бота (Backend Dev)
6. Прочитать `~/Desktop/qsnera-reels-bot/bot.py` полностью
7. Найти все handler'ы с кириллическими командами (`/меню`, `/чат`, `/задача`, `/отчёты` и любые другие)
8. Переименовать команды на латиницу по схеме:
   - `/меню` → `/menu`
   - `/чат` → `/chat`
   - `/задача` → `/task`
   - `/отчёты` → `/reports`
   - любые другие — по смыслу
9. Обновить BotFather если нужно — список команд через `/setcommands`
10. Проверить `analyzer.py` и `downloader.py` на наличие кириллических команд/хендлеров
11. Найти все места где бот называет себя "QSNera AI Bot" — заменить на "Axiom:Void Bot"
12. Проверить все строки в bot.py: `grep -n "QSNera\|qsnera\|QSнера" ~/Desktop/qsnera-reels-bot/bot.py`

### БЛОК 3: daily-self-dev.sh шаг 3 (DevOps + Backend Dev)
13. Прочитать `~/.claude/agents/daily-self-dev.sh` полностью
14. Найти шаг 3 — определить что именно он делает и какой путь использует
15. Запустить вручную только шаг 3 с verbose-выводом чтобы увидеть точную ошибку:
    ```bash
    bash -x ~/.claude/agents/daily-self-dev.sh 2>&1 | head -100
    ```
16. Проверить все пути в шаге 3 — найти пути содержащие `qsnera` или старые имена до миграции
17. Обновить пути: `qsnera` → `AxiomVoid` или актуальные пути согласно текущей структуре vault
18. Убедиться что шаг 3 проходит без `exit 1`: запустить скрипт ещё раз и проверить exit code

### БЛОК 4: Ночные падения vault-sync и local-agent (DevOps + QA)
19. Проверить системные логи на время ~02:00:
    ```bash
    grep -i "error\|fail\|exit" ~/.claude/logs/*.log 2>/dev/null | grep "02:"
    journalctl --since "yesterday 01:50" --until "yesterday 02:30" 2>/dev/null
    ```
20. Прочитать `~/.claude/agents/vault-sync.sh` и `~/.claude/agents/local-agent.sh`
21. Найти cron jobs или launchd plist связанные с этими агентами:
    ```bash
    crontab -l
    ls ~/Library/LaunchAgents/ | grep -i "vault\|claude\|axiom"
    ```
22. Определить что запускается в ~02:00 и может конфликтовать (Obsidian Sync, iCloud, Time Machine, резервные задачи)
23. Если найден конфликт — добавить retry-логику или сдвинуть время запуска агентов на 02:30+
24. Добавить в оба скрипта логирование с timestamp чтобы падения фиксировались:
    ```bash
    echo "[$(date '+%Y-%m-%d %H:%M:%S')] ERROR: ..." >> ~/.claude/logs/agent-errors.log
    ```

### БЛОК 5: Финальная проверка (QA Tester)
25. Запустить бот локально и проверить старт без ошибок:
    ```bash
    cd ~/Desktop/qsnera-reels-bot && python bot.py
    ```
26. Протестировать каждую переименованную команду
27. Проверить что бот представляется как "Axiom:Void Bot"
28. Запустить `daily-self-dev.sh` полностью и убедиться что все шаги проходят
29.
